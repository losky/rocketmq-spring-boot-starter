spring:
  application:
    name: test
  cloud:
    rocketmq:
      producer:
        name-server:
        access-key: a
        secret-key: a
      consumer:
        name-server:
        access-key: a
        secret-key: a
