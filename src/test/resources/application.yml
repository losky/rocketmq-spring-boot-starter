spring:
  application:
    name: test
  cloud:
    rocketmq:
      producer:
        name-server: b
        access-key: a
        secret-key: a
      consumer:
        name-server: b
        access-key: a
        secret-key: a
